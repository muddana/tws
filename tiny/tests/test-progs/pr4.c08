program hanoi:
var Disks:integer;

procedure Hanoi(a,b,c,n:integer);
begin
   if n>1 then Hanoi(a,c,b,n-1);
   output(a,b);
   if n>1 then Hanoi(c,b,a,n-1)
end Hanoi;

begin
     read(Disks);
     Hanoi(1,3,2,Disks)
end hanoi.
