program pr2e09:

# can't exit from a for loop.

var x,i: integer;
begin
   loop
      x := read;
      for i := 1 to 10 do
	 begin
	    output(i,x);
	    if i=x then
	      exit 
            else
	       output(999);
         end;
   pool;
end pr2e09.
